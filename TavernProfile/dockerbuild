# use := AUTHENTICATED USERNAME THAT CAN SEE THE TavernProvile-env REPO
# pwd := AUTHENTICATED USER PASWORD THAT CAN SEE THE TavernProvile-env REPO
# NEVER UNDER ANY CIRCUMSTANCES LEAVE PRODUCTION CREDENTIALS IN THIS FILE OR
#   OR THE DOCKERFILE!

docker build --build-arg NAME=use --build-arg PASS=pwd -t tavern-profile .
